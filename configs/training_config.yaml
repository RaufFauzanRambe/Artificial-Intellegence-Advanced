training:
  epochs: 100
  batch_size: 32
  learning_rate: 0.001
  optimizer: "adam"
  weight_decay: 0.0001
  scheduler: "cosine"
  early_stopping:
    patience: 10
    min_delta: 0.001
  gradient_clipping: 1.0
  num_workers: 4
